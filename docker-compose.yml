---
services:
  devcontainer:
    build:
      dockerfile: Dockerfile
      args:
        GO_VERSION: ${GO_VERSION:-1.24.4}
    volumes:
      - ..:/workspace:delegated
    init: true
    command: sleep infinity
